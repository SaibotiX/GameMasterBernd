# --- Credentials -----------------------------------------------------------
# Written by `pi-ai login` (OAuth access/refresh tokens) — never commit.
auth.json
app/auth.json
.env
.env.*
*.pem
*.key
credentials.json

# --- Runtime state ---------------------------------------------------------
# The ledger and everything the game downloads. Recreated on first run.
app/data/
data/

# --- Vendored binaries -----------------------------------------------------
# Static ffmpeg (~420 MB); place a build at IA/tools/ffmpeg/ffmpeg.
# (IA/tools/yt-dlp is a tracked git submodule — do not widen this to IA/tools/.)
app/tools/
IA/tools/ffmpeg/

# --- Dependencies ----------------------------------------------------------
node_modules/
dist/
*.tsbuildinfo

# --- Local tooling / editor ------------------------------------------------
.claude/settings.local.json
.vscode/
.idea/
*.swp
.DS_Store

# --- Playtest telemetry ----------------------------------------------------
# Testers' session drops are private play data; reports stay tracked.
IA/analysis/sessions-in/
