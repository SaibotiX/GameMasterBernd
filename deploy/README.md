# deploy/ — the stage-1 machinery

The buildable half of [02-friends-web-service.md](../research/roadmap/02-friends-web-service.md)'s checklist: the per-friend image (`image/`) and the host stack (`host/`). Law lives in the roadmap — 02 for architecture and auth, [08](../research/roadmap/08-stage1-web-ui.md) for the page, the R registry for every ruling; this file is the runbook: what to type, in which order, and what each step proves. Nothing in `deploy/` ever enters the image's game folder (the whitelist in `image/build.sh` is the whole list).

## The local loop (this machine, no purchases needed)

```bash
deploy/image/build.sh          # image from a git-archive whitelist of HEAD
deploy/image/verify.sh         # in-container: unit gate + three lenses live,
                               # pseudo-TTY probe, app-server probe (page,
                               # health, /ws/term stream), ttyd-fallback
                               # probe — all in the production read-only shape
deploy/host/localcheck.sh      # the whole door, production-shaped: TLS,
                               # secret path, basic auth, proxied WebSocket,
                               # hardened friend container
```

Both verify scripts fail loudly; green means what the headers say and nothing more. What no script can prove: how the TUI's dress *looks* in a real browser — that is the first-deploy eyeball sitting below.

## First deploy (after the errands)

Prerequisites, all maintainer errands (rulings R17/R18 name them): **worldconsole.eu** registered at INWX with A/AAAA records for `worldconsole.eu`, `play.` and `vault.` pointing at the box, DNS plain and unproxied; the **netcup VPS 500 G12 Vienna** on hourly billing, **click-AVV signed day 1**; the Hetzner runner-up account created and verified before it is ever needed.

*(State 2026-08-08, night: **the deploy stands** — steps 1–5 green on Debian 13 (docker 26.1.5, compose 2.26.1; `deploy` user, sshd keys-only, no core dumps, no swap, DOCKER-USER persisted via `worldconsole-firewall.service`); three hosts live under Let's Encrypt, AAAA rows live at INWX and propagated, deploy key pasted — **the box pulls on its own key**. Three landmines found and fixed en route, each its own commit: firewall drops learned container-origin scoping (`a0e6157`), the mint's urandom pipes got bounded input (`a398e69`), caddy mounts the `caddy/` directory instead of a pinned single-file inode (`e99885b`). The maintainer's own door is minted and green from outside (401 bare / 200 authed, ttyd page; `wc-tobias` healthy; pair handed out of band). Since 2026-08-09 friend state is box-local and gitignored — `caddy/friends/tobias.caddy` + `compose.override.yaml` — and the box's tracked tree is CLEAN: pulls need no stash dance. Access: `ssh -i ~/.ssh/worldconsole root@152.53.51.13` (root or `deploy@`); clone at `/home/deploy/world-console`. Ops split (learned 2026-08-09): pull/build/compose run as `deploy@` — as root they trip git's safe.directory guard and the archive fails; the store and `gateway-state/` are root's (deploy has no interactive sudo). **Closed 2026-08-09:** the eyeball sitting ran green — Firefox and a Chromium-family browser, dress, space-to-cast, board urgency, bell, media announcements and the `/pick`/`/roll` paths all as expected. The external-vantage IPv6 check is **on ice** (maintainer's call, 2026-08-09): parked here, its owning note — revisit at the next natural v6 vantage (a new PC, a phone-on-mobile-data moment) or on the first report of an unreachable door; proven on-box, and browsers' happy-eyeballs fall back to v4 regardless. The first-deploy round is complete.)*

*(State 2026-08-09, app-server round: **the page is live** — `wc-tobias` recreated onto the app-server image; verify's four legs green on the dev machine AND the box (the box has no host node by design — verify lends the image's own, learned on the first on-box run); localcheck walks the full page through a production-shaped door. Live smoke: page 4 ms, tree 50 ms, `constitution.md` 4 ms in-container; door shape from outside 200/404/401 as designed; the watcher hop measures ~26 ms isolated on the dev machine. pi now spawns on first attach — an idle container carries no game process. First eyeball (2026-08-09): panes passed; the terminal was dark and deaf — ONE cause, `#overlay`'s id rule outweighing `[hidden]`, an invisible veil darkening the TUI and eating every click; fixed and re-deployed the same day (`61cf91b`), and the probe now TYPES so the input direction is never untested again. **Closed 2026-08-09:** the re-eyeball ran green — 08's laws held in the real browsers (typing and space-to-cast through the page, boot text, tabs, the glass's auto-open, the ledger's tail), everything as intended; item 5 closes and the round is complete. Item 12's ruling landed the same day: the host half rides the shipper round (§Status).)*

*(State 2026-08-09, shipper round: **sessions ship themselves** — R13's lattice is live end to end. The shipper rides the app server's seams (boot sweep, 10-minute checkpoints, seal at pi's exit and at the stop signal), writing each friend's own staging slice; store-sweep verifies every manifest hash and compacts to `sessions/<player>/<sid>.tar.zst` on the daily timer; the reaper stops a friend after 30 clientless minutes — in the live receipt the stop grace itself carried the seal — and the waker answers sleeping doors, proven from the real outside door: waking page, the real page one knock later. First real act on deploy: **tobias's 10-session back-catalog shipped on boot and compacted clean** (68 files, 0 errors). The analysis pull landed a session in the kit's `sessions-in` shape over the wire, and a store-side deletion propagated to the mirror on the next pull; the two probe friends that carried these receipts were minted, shipped and fully removed on production machinery. Box migration: tobias's override gained `WC_PLAYER` + the `/ship` bind, his door the waker upstream; verify's five legs green on the box; both timers armed (`worldconsole-reaper` 5-min, `worldconsole-store-sweep` daily 04:19). One landmine en route: a stopped container's DNS lookup can HANG instead of NXDOMAINing (dev machine's DNS quirk) and eat caddy's try window — `dial_timeout` in the door snippet bounds it (`7c5a8cc`). The maintainer's restart eyeball caught a second one the same evening: the app server spawned pi PLAIN, so every wake began a blank console — 02 §sizing had promised "pi resumes the sitting" all along; fixed with `--continue`-when-a-session-exists (the shipper already re-seals grown sessions by design), and localcheck now proves the woken console replays the sitting. The `/login`-again-after-wake is NOT a bug: R11's tmpfs wipe, cured for friends by the house lane (item 8). **Ruled 2026-08-09, both riders:** the plain 0700 store stands (§the store) and the disk watch stays alarm-only, pruning iced (§the reaper). Seen-before-done: your own console now sleeps after 30 idle minutes and wakes on your next visit — the waking page and the stop/start cycle await your eyes. Seen 2026-08-09: green — the shipper round closes whole.)*

*(State 2026-08-09, house-lane round: **the lane is live** — R29's key holds it, the gateway (deploy/host/gateway/) stands on `web` with the ledger inside: calendar-month grants, the ruled $55≈€50 monthly kill-switch and $5.50≈€5 daily alarm, ntfy pings from the box-minted topic (read it: `ssh deploy@box 'grep NTFY_TOPIC world-console/deploy/host/.env'` — subscribe in the ntfy app), nightly reconciliation at 04:47 against pi's own cost stamps. tobias's door rides the lane (virtual key + `WC_MODEL` in the override; his env key means `/login`-after-wake is CURED — nothing to log into), and the drain test ran live on production: a 50-micro grant bought exactly one real turn, the second knock got the friendly refusal, the ping fired, and the reconcile preview read both meters agreeing (Δ171 micro under the dust floor). The org key sits in the box's 600 `.env` and the password manager, nowhere else; `gateway-state/` is 700, uid-1001, box-local. **Closed 2026-08-09:** the played turn ran green — rounds counted down in the strip, and after the sleep the next knock woke the console mid-sitting, no login asked (the cure, seen); the maintainer subscribed to the ntfy topic. The same turn caught the lane's one gap: every gmchat side call (GM table, chronicler, fate planner, truth judges, crafting) still knocked on api.anthropic.com with the virtual key — /gm and /history long answered 401, and the fail-open planner would have skipped twists silently. Fixed the same day: `laneModel` in `extension/gmchat.ts` rides WC_GATEWAY_URL exactly like pi's own turns; its own seen-before-done — /gm answering, /history long telling the saga ON the lane — landed green 2026-08-09: both behaved as expected, the rider closes. Side-call spend now meters through the gateway (caps correct by design); the reconcile question that raises is filed in roadmap 09's house-lane row for the maintainer's ruling.)*

*(State 2026-08-09, words round: **the words are live** — the landing ground serves at the root (index with the loud disclosure, the Art. 50(1) sentence and both names for R16's dated public use · `datenschutz.html` against R29's re-verified facts — "deleted within 30 days at most", SCCs as Anthropic's transfer ground, netcup Vienna named · `impressum.html` rendering R19's address from the box `.env` alone). **The address errand closed 2026-08-09** — `WC_IMPRESSUM_ADDRESS` set in the box `.env`, caddy recreated, the rendered address verified on the live page (all three pages 200, the door shape untouched); the R16 first-use set landed box-local, dated, with checksums (`deploy/host/first-use/2026-08-09/`, gitignored — the rendered Impressum carries the address the repo never holds), and the web-archive save was **stopped on the maintainer's call** (both public archives walled the box; receipt + the ready click-links in 06 §2026-08-09). The play page speaks before first play — the first-run notice once per browser, the 50(2) marking as page meta + `x-ai-generated` on chronicle prose. The intro is ready to send (`deploy/friend-intro.md`), the consent gate refuses rowless mints — proven live — and §deletion below dry-ran green end to end on production with `probe-del`: gate refusal · a real lane turn · seal/sweep/pull landed · full erase · the mirror propagated the deletion on the next pull · the consent row marked withdrawn. The side-call tag is deployed (gmchat's voices ride `/side`, the ledger tags, reconcile subtracts — 09's question ruled and closed). Box state: image `ecfcdfa`, every service recreated (the gateway force-recreated — its single-file bind pins the inode, the caddy lesson applies), verify green on the box. **Closed 2026-08-09: both eyeballs green** — website, legal pages, Impressum, starting page and the one-time first-run notice all as expected (the maintainer's own words). The words round closes whole; the first invite waits on item 13's backup round alone, the ruled gate.)*

*(State 2026-08-10, backups round: **the lane is live and the invite's gate fell** — borg 1.4.0 on the box, the repo encrypted repokey-BLAKE2b on the Storage Box (BX11 Falkenstein; host keys pinned in root's known_hosts, all three verified against Hetzner's published fingerprints). The maintainer's at-order paste turned out to be the box's own `storagebox_ed25519` pubkey, so the lane opened keys-only from the first minute — the box password never left the manager, exactly as 06 §2026-08-09 ordered. First archive `nightly-2026-08-09T22-27-52Z`: five volumes staged read-only + the store + all six box-local state paths — 60 files, 4.56 MB → 3.84 MB encrypted — prune + compact clean, the timer armed for 05:11 nightly (Persistent). **The restore gate ran green the same night against the real repo:** data-tobias's tar extracted from the archive, rebuilt into a scratch volume, a scratch container booted against it at the game's own data path, and the chronicle `world/dragon-realm/…/ledger.md` read whole — sha256 identical to the live volume (`bc49d259…`). Passphrase custody: `/root/worldconsole-borg.pass` + the key export `/root/worldconsole-borg.key`, both root-600; the maintainer's one errand: read both into the password manager (§Backups carries the command). Two landmines en route, each its own commit: the minted script landed without its exec bit (`227edb7`), and the staging/restore containers needed `--user 0` — the player uid can neither write the root-700 staging dir nor read caddy's root-owned volume files (`518c0bb`). One stray found riding along: `world-console_sessions-drain-test`, the house-lane round's probe volume, still exists and enters every archive — flagged for the maintainer's call, not deleted.)*

On the box (Debian stable assumed):

1. Base: `apt install docker.io docker-compose-v2 git` (or Docker's own repo), a non-root deploy user in the `docker` group, SSH keys only.
2. Clone the repo (read-only deploy key), `cd deploy/host`, write `.env` with `ACME_EMAIL=<a real mailbox>` (gitignored; the mail-on-domain ruling is still open — any working mailbox serves ACME).
3. Host hardening that makes R11's wipe real (02 item 2): core dumps off (`kernel.core_pattern=|/bin/false` + `* hard core 0`), **no swap** (netcup images ship none — verify with `swapon --show`; if any exists, `swapoff -a` and remove from fstab, or encrypt it).
4. `deploy/host/firewall.sh` — after docker is up; persist it (a oneshot systemd unit `After=docker.service`, or iptables-persistent). Verify from inside a container: `docker run --rm world-console:latest node -e "fetch('http://192.168.1.1',{signal:AbortSignal.timeout(5000)}).then(()=>process.exit(1)).catch(()=>process.exit(0))"` (drop = timeout = pass; a reply = the block is not standing).
5. `deploy/image/build.sh` on the box, then `docker compose up -d caddy`. Prove the door from *outside*: `curl -sI https://worldconsole.eu` (and `play.`/`vault.`) — 200/404 under a Let's Encrypt issuer. The in-container egress probe cannot see an inbound break; the first deploy learned that the hard way.
6. **The eyeball sitting** (closes 02 items 3–4's browser half): mint a door for yourself, open it in Firefox and one Chromium-family browser, play a short real sitting. Check: dice overlay dress and space-to-cast, the four-slot board's red urgency, the bell, media announcements, `/pick` and `/roll` as the guaranteed paths where Alt+number is browser-bound (Firefox on Linux switches tabs — expected, not a bug).

## Per-friend onboarding

The words came first (02 items 9+11, the words round) — what remains is the ORDER, and new-friend.sh enforces its gate:

1. **Invite:** send [friend-intro.md](friend-intro.md) part one, by any channel that keeps the reply.
2. **Record the yes** (02 item 9, R13/R20): both questions answered plainly → one row in the box-local, gitignored `deploy/host/consents.md` — `| alice | 2026-08-12 | signal | yes | yes | note 2026-08-09 |` (name · date · channel · recorded-consent · 18+ · privacy-note version). The friend's actual words stay in the channel they came by; the row records the fact. Probe friends get a row with channel `probe`.
3. **Mint and open:**

```bash
deploy/host/new-friend.sh alice        # refuses without alice's consent row
docker compose up -d gateway wc-alice
docker compose exec caddy caddy reload --config /etc/caddy/Caddyfile
```

4. **Send part two** — the printed door + pair, out of band, once.

*(The gate fell 2026-08-10: §Backups' restore test ran green against the REAL Storage Box repo — the backups-round state note carries the receipt. Nothing stands between the words and the first invite.)*

Friend state is box-local and gitignored — `caddy/friends/<name>.caddy` (imported by the play site block) plus `compose.override.yaml` (auto-merged; each friend `extends` the tracked `wc-template`) — so the tracked tree never carries a friend and `git pull` never needs a stash dance. Removing a friend without erasing them (a pause, a broken door): `docker compose down wc-<name>`, delete the snippet and the override lines; volumes and store stay. Erasing them is §deletion below.

## Updates (the path that cannot destroy a chronicle)

```bash
git pull
deploy/image/build.sh
docker compose up -d               # recreates changed friend containers
```

If the app server ever misbehaves, the bare-ttyd rung is one compose
`command:` override away (the Dockerfile's CMD note carries the exact line —
R14 keeps the fallback aboard, and verify leg 4 keeps it honest).

Named volumes (`data-*`, `sessions-*`) are untouched by recreation; chronicles survive by construction. A **pi version change is never an update** — it is an upgrade and takes the full rite ([pi-upgrades.md](../research/design/pi-upgrades.md)) before the new pin builds anyone's container.

## The store (02 item 10, R13 — where sessions become research data)

One private place on the box, root-only: `/srv/worldconsole/store/` (created at the shipper deploy: `install -d -m 700 -o root -g root /srv/worldconsole/store /srv/worldconsole/store/staging /srv/worldconsole/store/sessions`; the box needs `zstd` for compaction — `apt install zstd`).

```
store/staging/<player>/<sid>/     the friend's OWN slice, bind-mounted at
  session.jsonl · story/**        /ship in their container (minted by
  manifest.json · sealed          new-friend.sh, owned by the player uid) —
                                  the in-container shipper writes here and
                                  can reach nothing else of the store
store/sessions/<player>/<sid>.tar.zst
                                  the compacted record, root-only — written
                                  by store-sweep.sh after verifying every
                                  manifest hash; staging's data files are
                                  then pruned, its manifest + sealed markers
                                  stay (they answer "shipped already")
```

Shipping is the image's job at its seams (boot, 10-minute checkpoints, pi's exit, the stop signal — `appserver/shipper.js`); the store side is the host's (`store-sweep.sh`, daily timer: `cp deploy/host/systemd/worldconsole-store-sweep.{service,timer} /etc/systemd/system/ && systemctl daemon-reload && systemctl enable --now worldconsole-store-sweep.timer`). The analysis machine pulls `sessions/` — never `staging/` — with the kit's puller (`research/analysis/tools/pull-sessions.sh`), landing straight in the `sessions-in` layout.

Ruled 2026-08-09 (closing the ⚠ DEVIATION, R13 / 02 §research data): **the plain 0700 root-only directory stands.** At-rest encryption with an on-box key guards nothing the live plaintext volumes don't already surrender — the honestly-encrypted copy is the borg backup (item 13, key off-box), and the store's real walls are file mode + the box's SSH door. 02 §research data carries the dated revision; a gocryptfs mount can still slot under the same path if the question ever re-opens (e.g. the store outgrowing the box).

## The reaper (02 item 12's host half — rode the shipper round per the 2026-08-09 ruling)

`reaper.sh` every 5 minutes (`cp deploy/host/systemd/worldconsole-reaper.{service,timer} /etc/systemd/system/ && systemctl daemon-reload && systemctl enable --now worldconsole-reaper.timer`): any friend 30 minutes without a WebSocket client is stopped — the stop grace lets the app server seal, then `store-sweep.sh --friend` makes the seal a certainty and the compact immediate. An idle container costs nothing extra: pi only spawns on attach, and the next connect resumes the sitting — worlds persist, and pi `--continue`s the conversation whenever a prior session exists (02 §sizing's promise; the shipper re-seals a resumed session by design). What does NOT survive a stop is `auth.json` — that is R11's tmpfs wipe working, not a bug; the house lane (item 8) is the designed cure for friends. The same pass watches per-volume disk (`du` warn at 2 GiB, alarm at 5 GiB — **alarm-only by ruling, 2026-08-09**: automatic pruning of `data/downloads/` deletes player-visible files on a machine's judgment and is declined — iced with its revival triggers, `research/icebox.md`) and prints one `docker stats` line per running friend into the journal.

**Start-on-connect is the waker** (`host/waker/`, a compose service — `docker compose up -d` carries it): every minted door lists it as the second upstream under `lb_policy first`, so a knock on a sleeping container serves an auto-refreshing "the console is waking" page while the container starts by name over the docker socket. The socket rides in a container on the `wake` network that caddy alone shares — friends on `web` cannot route to it, and the waker's whole vocabulary is "start `wc-<validated-name>`"; a removed friend's knock gets the "closed" page instead.

## Backups (02 item 13, R18 — the lane built 2026-08-10)

Nightly at 05:11 (`worldconsole-backup.timer` → `backup.sh`, root, after the 04:17 store sweep has compacted the day's sessions): every `world-console_*` named volume is staged read-only as a plain tar (borg compresses; plain tars dedup across nights), then ONE borg archive carries the staged tars + `/srv/worldconsole/store` + the box-local gitignored state — `consents.md`, `.env`, `gateway-state/`, `caddy/friends/`, `compose.override.yaml`, `first-use/` — onto the **Hetzner Storage Box** (BX11 Falkenstein, cross-provider per R18; at-purchase facts 06 §2026-08-09) at `ssh://u648152@u648152.your-storagebox.de:23/./borg/worldconsole`. `auth.json` is excluded **by construction**: it lives on the tmpfs agent dir (R11), never inside any volume, so no backup can contain it.

Custody: the box's dedicated backup key `/root/.ssh/storagebox_ed25519` opens the Storage Box — its pubkey was pasted at order, so the lane has been keys-only from the first minute; the Storage Box host keys are pinned in root's `known_hosts`, verified against Hetzner's published fingerprints (2026-08-10); the box password rests in the password manager, never typed into any terminal. The repo is `repokey-blake2`: the passphrase sits root-600 at `/root/worldconsole-borg.pass`, and its recovery copy lives OFF-box in the password manager — read it yourself, off-transcript: `ssh -i ~/.ssh/worldconsole root@152.53.51.13 cat /root/worldconsole-borg.pass`. Box + passphrase from the manager = full restore anywhere; the `.env` (org key) and `gateway-state/` ride inside the encrypted repo on purpose — disaster recovery needs them.

**The month promise (§deletion's binding constraint):** `borg prune --keep-within 28d` + `borg compact` every night — an erased player ages out of every archive inside the month the privacy note promises, never by hand-editing archives; Storage-Box automated snapshots stay **OFF** (06 §2026-08-09) so no layer outlives the prune. Install on the box: `apt install borgbackup`, then `cp deploy/host/systemd/worldconsole-backup.{service,timer} /etc/systemd/system/ && systemctl daemon-reload && systemctl enable --now worldconsole-backup.timer`.

The restore test — extract the latest archive, tar a data volume back into a scratch volume, boot a scratch container against it, prove a chronicle actually reads — is the exit gate: proven once before the first friend, again per the stage-2 gates. Localcheck proves the whole cycle keyless against a throwaway file repo — stage, create, the 28-day prune eating a planted stale archive, and the restored chronicle reading — wherever borg is installed (the dev machine lacks it today: the leg skips LOUDLY there, and the box's nightly run carries the standing proof).

## Deletion (02 item 9's tail, R13 — "we delete your sessions within a month")

The request arrives by any channel (at invite scale the requester IS the friend); withdrawal of consent is the same path. Everything below is on the box unless said otherwise, `<n>` the friend's name. Dry-run once before the first friend — the receipt lives in the state note.

1. **Close the door:** `docker compose down wc-<n>` · delete `caddy/friends/<n>.caddy` · remove the `wc-<n>:` service block AND the `data-<n>:`/`sessions-<n>:` volume lines from `compose.override.yaml` · `docker compose exec caddy caddy reload --config /etc/caddy/Caddyfile`. Their knock now gets the by-invitation 404.
2. **The play volumes:** `docker volume rm world-console_data-<n> world-console_sessions-<n>`.
3. **The store:** `rm -rf /srv/worldconsole/store/staging/<n> /srv/worldconsole/store/sessions/<n>` (root).
4. **The lane:** remove their key from `gateway-state/keys.json`, then anonymize their ledger rows — the spend sums are the billing truth (R12) and stay, the handle goes:

```bash
docker run --rm --user 0 -v "$PWD/gateway-state:/d" -e P=<n> world-console:latest node -e '
  const fs=require("fs");
  const keys=JSON.parse(fs.readFileSync("/d/keys.json","utf8"));
  for (const [k,v] of Object.entries(keys)) if (v.player===process.env.P) delete keys[k];
  fs.writeFileSync("/d/keys.json", JSON.stringify(keys,null,"\t")+"\n");
  if (fs.existsSync("/d/usage.jsonl")) {
    const done=new Set(fs.readFileSync("/d/usage.jsonl","utf8").split("\n").filter(Boolean)
      .map(l=>JSON.parse(l).player).filter(p=>/^deleted-/.test(p)));
    const alias=`deleted-${done.size+1}`;
    const out=fs.readFileSync("/d/usage.jsonl","utf8").split("\n").filter(Boolean).map(l=>{
      const r=JSON.parse(l); if (r.player===process.env.P) r.player=alias; return JSON.stringify(r);
    }).join("\n")+"\n";
    fs.writeFileSync("/d/usage.jsonl", out);
  }' 
docker compose up -d --force-recreate gateway   # drop the folded in-memory buckets
```

5. **The analysis mirror** (dev machine): `research/analysis/tools/pull-sessions.sh` — the store mirror (`research/analysis/store-mirror/`) propagates deletions on the next pull (rsync --delete, proven in the shipper round). Landed `sessions-in/<batch>/<n>` copies are the tool's never-touched working dirs — remove them by hand; their batch's immutable report falls under step 8's rule.
6. **The consent row** (`deploy/host/consents.md`): append `withdrawn <date>` to their row — it stays as Art. 7(1) proof that processing until then was lawful, and dies with the register at the beta's end (the papers' VVZ B).
7. **Backups:** nothing to do by construction once item 13 honors the ≤ 28-day retention above — the erased player ages out of every archive within the promised month. Until item 13 exists there are no backups to purge.
8. **The reports** (`research/analysis/reports/`, immutable, R4-tier internal): raw sessions are the promise's scope and are now gone everywhere. If a request explicitly extends to quoted lines inside a report, that is immutability vs. erasure — surface to the maintainer for a ruling, never resolve silently.

## Status (kept honest, updated per round)

| Piece | State |
|---|---|
| Image + in-container verify | built & green on this machine (2026-08-08) |
| Host stack + local door check | built & green on this machine (2026-08-08) |
| Purchases (domain, box, AVV) | done (2026-08-08) — worldconsole.eu at INWX, netcup Vienna box, click-AVV concluded |
| First deploy + eyeball sitting | done (2026-08-09) — deploy 2026-08-08, eyeball green in both browser families; v6 vantage check on ice (§first-deploy state note) |
| App server & panes (02 item 5, R14) | done (2026-08-09) — probes green dev + box, tobias's container recreated onto it; the pane-page eyeball ran green — the round is complete |
| Shipper + item 12's host half (items 10 + 12, R13) | done (2026-08-09) — mirror/seal/sweep live at every seam, store verified + compacted daily, reaper + waker + disk watch armed; tobias's back-catalog shipped on first boot; pull and deletion proven end to end (§shipper-round state note); both riders ruled 2026-08-09 — the plain store stands, the watch stays alarm-only; the wake-cycle eyeball green 2026-08-09 — closed whole |
| House lane (item 8, R12/R29) | done (2026-08-09) — gateway + ledger + tripwires + strip + reconciliation, drain test green on production; the played turn seen green 2026-08-09 — the round closes whole (§house-lane state note). Rider closed: the side-call 401 caught at that turn is fixed (gmchat rides the lane); its lane eyeball ran green 2026-08-09 — /gm and /history long behaved on the lane |
| Disclosure + intro (items 9 + 11) | done (2026-08-09) — the words round closed whole: landing ground live with the address (env set 2026-08-09), first-run notice + 50(2) marking in the page, intro ready with the LICENSE line, consent gate live, §deletion dry-run green on production, side-call tag deployed, first-use set archived box-local (web-archive save stopped by ruling — 06 §2026-08-09); both eyeballs green 2026-08-09 (§words-round state note); vault (items 6–7) on ice with R11 |
| Backups + restore test (item 13) | **the next round — gates the first invite** (ruled 2026-08-09): Hetzner Storage Box purchase (maintainer errand) + borg (retention ≤ 28 days per §deletion) + one proven restore — the map: §Next round below, prepped 2026-08-09 |

## Next round (prepped 2026-08-09): the backups — 02 item 13, the invite's gate

The words closed whole, so item 13 is the ONE gate left before friend #1 (ruled 2026-08-09; §per-friend onboarding holds the order). The shape is §Backups' + R18's; the at-purchase facts — the BX11 pick, the port-23 lane, the two toggles, the snapshot rule — are re-verified in 06 §2026-08-09. The map (commit boundaries per R9):

1. **The maintainer's errand, first:** buy the Storage Box — **BX11, Falkenstein** — on the verified runner-up account; set its password (password manager); switch ON **SSH support** and **external reachability**; conclude the Hetzner AVV ([10-privacy-papers.md](../research/roadmap/10-privacy-papers.md)'s pending row flips, its own records line). The session needs exactly one fact back: the `uXXXXXX` username — the host derives (`uXXXXXX.your-storagebox.de`).
2. **The borg lane lands:** a dedicated root keypair on the box → the Storage Box's `authorized_keys` (one password-auth install, keys-only after); `borg init` with the repo passphrase OFF-box in the password manager (the honestly-encrypted copy, §the store's ruling); the nightly timer stages volumes read-only per §Backups' tar line + the store + **the box-local gitignored state — `consents.md`, `.env`, `gateway-state/`, `caddy/friends/`, `compose.override.yaml`, `first-use/` — found at prep in NO backup path, though consents.md is the Art. 7(1) proof and gateway-state/ the R12 billing truth**; `borg prune` ≤ 28 days (§deletion's binding constraint — and Storage-Box automated snapshots stay OFF, so the aging-out promise binds every layer). ~2 commits (lane + timer · the check legs).
3. **The restore test — the exit gate:** archives back into a scratch volume, a scratch container boots against it, a chronicle actually reads; §per-friend onboarding's standing-gate line flips with the receipt in the state note. 1 commit + the wrap.
4. **R18's second leg, shape decided in-round:** the periodic pull of the backup to the maintainer's machine (distinct from the analysis mirror's session pull) — smallest honest shape.

Then the invite: §per-friend onboarding 1–4, [friend-intro.md](friend-intro.md) part one.

<details><summary>The house-lane round's map (picked 2026-08-09, closed the same day — kept for the round's archaeology)</summary>

Both open fronts gate friend #1 (the onboarding gate above, plus 02 §checklist's preamble: with the doors iced, even friend #1 rides the house lane) — so the pick is an *ordering*, and the order is dependency's: the lane fixes the very facts the words must state — which provider is named recipient in the privacy note, what cap and `/limits` the intro explains, what the strip shows — so the words round (items 9 + 11 + the first invite) follows this one and writes true sentences once, against terms re-verified days before a friend reads them. The lane also carries the round-sized technical unknowns worth surfacing early (gateway, cache passthrough, reconciliation), and its receipt is playable by the maintainer alone. The map (commit boundaries per R9):

0. **Gate zero — the house-key ruling lands as its own R entry** (06 §2026-08-08 the house key recommends **Anthropic direct, prepaid**; the maintainer confirms the early-minted org key is exactly that, or rules otherwise), executing the at-account-creation re-verify trigger (prices, rate-limit tier, retention pages, spend-cap mechanics) and confirming the workspace spend cap is SET (item 8's standing note). One records commit; the coverage row's open edge closes. *(Done 2026-08-09: **R29** — the maintainer confirms the minted key is exactly the recommendation and the cap is set; all four re-verify legs held against the live pages (06 §2026-08-09 — one new ⚠: fresh orgs may start in an "Evaluation tier" below Start). The coverage row closed.)*
1. **Gateway spike → shape decision:** adopt LiteLLM vs a small own proxy ("LiteLLM-style" in R12 names a shape, not a dependency). The spike proves, against the real API on a tiny balance: pi speaks through it unmodified (base URL + virtual key), `cache_control` passes through with cache hits on the receipt (caching IS the cost model — 06's table), streaming intact, per-turn usage captured. Decision + receipts, one commit. *(Done 2026-08-09: **the own proxy is adopted** — ~200 lines of the node the image already carries, metering in exactly the rows the ledger needs at R29's re-verified prices. All five legs green against the REAL API (`deploy/gateway-spike/`, ~1.3¢ total): a turn flows · streaming intact · cache passthrough EXACT — 7,981 tokens written, 7,981 read back, and the next run's first call read the prior run's cache · a 1-micro grant drains and the next knock gets the friendly 400 · pi itself answered through the 3-line `.pi/extensions` override on a virtual key, trust pre-answered as the entrypoint already does. Harvest: the live usage shape now splits `cache_creation` into 5m/1h buckets (precise write pricing for the ledger round). LiteLLM declined — virtual keys require Postgres, its own cost tables and admin surface would hold the org key, two more services on a 4 GB box for nothing the lane needs; iced with revival conditions, `research/icebox.md`. The spike's LiteLLM harness stays aboard for any future head-to-head.)*
2. **The gateway lands:** a compose service; per-friend virtual keys with budget + rate limit; the org key reaches the box only as a gitignored `.env` secret — never the repo, never an image layer. Localcheck learns the leg: a turn flows, a cache hit shows, an exhausted budget refuses. ~2 commits (service + wiring · the checks). *(Done 2026-08-09, two commits: the spike's proxy grew into `deploy/host/gateway/gateway.js` — bind env-driven, keys re-read per request, per-key rpm ceiling — seated on `web` as a capability-stripped uid-1001 service with box-local gitignored `gateway-state/`; new-friend.sh mints the lane trio (virtual key at the $10 placeholder grant + `WC_GATEWAY_URL` + `WC_MODEL`), the app server pins the keeper's model from env, and the game ships the 3-line override in `.pi/extensions/gateway.ts`. Localcheck gained five keyless legs against a pretend-Anthropic stub — turn, cache hit, dry-grant refusal, **pi's own turn through the lane in the hardened container**, meter — all green alongside every standing leg.)*
3. **The ledger (R12):** per-player balance, append-only transactions, test-phase grants with the hard per-player cap, the global daily spend alarm, the monthly kill-switch that never comes off; nightly reconciliation against pi's per-turn cost lines (R6). Grant math opens from the cache-adjusted estimate (06 §the house key) until real telemetry replaces it. ~2–3 commits (core · alarm + kill-switch · reconciliation with its first receipt). *(Done 2026-08-09, two commits. The ledger lives IN the gateway: calendar-month grants ($10/month placeholder), boot-folded buckets, and the philosopher's ruled bounds — kill-switch $55≈€50/month resting the whole lane, daily alarm $5.50≈€5 speaking only; pings by ntfy topic (spend numbers, never play), firing at record time and at boot; `/grant` shows each friend their own window alone. Reconciliation: `reconcile.sh` + nightly 04:47 timer re-derives each day per player from pi's own `usage.cost.total` stamps in the sessions volumes and exits red on >5% drift — first receipt in localcheck: fabricated matching meters agree, a ghost ledger row with no sessions volume is called out. Tripwires proven by shrinking caps: the lane rests, both pings land, ruled values reopen it.)*
4. **The strip shows the rounds** (08's status-strip law): remaining rounds visible in the page. One commit. *(Done 2026-08-09: the app server's `/api/grant` asks the gateway with the container's own virtual key and hands the browser ROUNDS only — money and keys never cross into the page; the strip note reads "~N rounds" / "grant spent" / "the keeper rests" on a 90-second clock, stays absent in laneless play, and the probe holds the window's shape in both lives. Localcheck read ~111 rounds through the real door; verify green off-lane.)*
5. **Live proof, seen-before-done:** tobias's door rides the lane first — also curing his `/login`-after-wake (§the reaper) — and a test grant is deliberately drained to prove cap → friendly stop → maintainer ping (stage-exit machinery, proven early). Box migration + wrap state note, push. The receipt for the maintainer's eyes: a played turn on the lane with the rounds count moving. *(Machine half done 2026-08-09 — §house-lane state note. Seen 2026-08-09: green — rounds moved, the wake replayed the sitting loginless; the round closes whole. The turn's catch — the side-call 401 — is fixed the same day and awaits its own lane eyeball in the words round.)*

Riders into the round: the wake-cycle eyeball landed green 2026-08-09 (§shipper-round state note — that rider retires) · 2026-08-31 ends Sonnet's intro pricing — binds only if Sonnet enters the mix; the keeper stays the tuned Haiku by every receipt, and the model mix is design + telemetry's question, outside gate zero's ruling.

</details>
