# 02 item 12's host half — runbook §the reaper.
# Installed by: cp to /etc/systemd/system/, daemon-reload, enable --now the timer.
[Unit]
Description=GameMaster Bernd reaper (stop-on-idle with seal, disk watch, stats line)
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
ExecStart=/home/deploy/gamemaster-bernd/deploy/host/reaper.sh
