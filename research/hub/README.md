# The hub — migrated

This folder's five documents (architecture · wiring workflow + tenant
contract · build plan · research log · overview) migrated WHOLESALE at
§M0 (2026-08-20; R35/R36) to the hub's own repo:

**World Console** — `~/Desktop/CurrPC/Programming/WorldConsole`
(private, `SaibotiX/WorldConsole`), design home `hub/`, its own records
under `records/` citing R34–R36.

Nothing hub-design lives here anymore; links into this folder from
dated registry entries land on this stub by design. One boundary
stands until the H1 ingress cutover: the LIVE box still runs whole
from THIS repo, and `deploy/README.md` remains its runbook (the hub
repo's CLAUDE.md carries the same caveat from its side).
